import React from "react";
import { Link } from "react-router-dom";
import "../../App.scss";

const Header = () => {
  return (
    <nav className="header">
      <h2>bip.so</h2>
    </nav>
  );
};

export default Header;
